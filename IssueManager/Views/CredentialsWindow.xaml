<Window
    x:Class="IssueManager.Views.CredentialsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Jira Credentials"
    Width="400"
    Height="270"
    WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Margin="0,0,0,5" Text="Email:" />
            <TextBox x:Name="EmailTextBox" Height="25" />

            <TextBlock Margin="0,10,0,5" Text="API Token:" />
            <PasswordBox x:Name="TokenBox" Height="25" />
        </StackPanel>

        <Button
            Grid.Row="1"
            Margin="0,15,0,0"
            Click="OpenApiSite_Click"
            Content="Generate API Key Online" />

        <StackPanel
            Grid.Row="3"
            Margin="0,0,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="75"
                Margin="0,0,10,0"
                Click="Save_Click"
                Content="Save" />
            <Button
                Width="75"
                Click="Cancel_Click"
                Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>
